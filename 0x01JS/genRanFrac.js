Math.random() //returns a random number between 0 and one but it cannot be one
Math.floor(Math.random() * 20); //floor rounds down to the nearest whole number

function randomWholeNum() {

    return Math.random();
}

console.log(randomWholeNum)